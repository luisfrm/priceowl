[project]
name = "priceowl"
version = "0.1.0"
description = "Price tracking bot"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/*",
    "packages/*"
]
